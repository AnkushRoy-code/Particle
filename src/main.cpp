#include "App.h"

// if you want to undertand/work on this watch the src files and the header
// files if you want to know how the code is structured I comented code in
// those. IDK what the norm is i did what I thought was right.
int main()
{
    App app;

    app.RunEngine(app);

    return 0;
}
